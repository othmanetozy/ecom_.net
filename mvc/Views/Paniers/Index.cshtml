@model IEnumerable<mvc.Models.Panier>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.prix_total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nombre_article)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descritption)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.prix_total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_article)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descritption)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
